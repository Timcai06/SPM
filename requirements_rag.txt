# RAG 模块依赖

# 核心依赖（必须）
psycopg                 # PostgreSQL Python 驱动

# 向量存储
pgvector                # PostgreSQL 向量扩展（需要单独安装）

# Embedding 生成（可选，选择其一）
sentence-transformers   # 本地 embedding 模型（推荐）

# LLM（可选，选择其一）
# Ollama 已安装，直接用 HTTP API 即可

# PDF 解析（可选，用于加载赛题 PDF）
pdfplumber

# LangChain（当前版本已安装但未使用，可删除）
langchain
langchain-community
langchain-core
langchain-text-splitters
langchain-classic

# 以下未安装且不需要（当前方案用 PostgreSQL array 存储向量）
chromadb
torch